services:
  yolo-studio:
    image: yolo-studio:latest
    build: .
    ports:
      - "5016:5016"
    shm_size: "2gb"
    environment:
      - CPU_THREADS=14
      - OMP_NUM_THREADS=14
      - MKL_NUM_THREADS=14
      - YOLO_OFFLINE=1
      - HF_HUB_OFFLINE=1
      - TRANSFORMERS_OFFLINE=1
      - APP_PORT=5016
    volumes:
      # CentOS SELinux 需 :z
      - ./data:/app/data:z
      - ./weights:/app/weights:z
    restart: unless-stopped
